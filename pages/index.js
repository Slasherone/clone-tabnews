// default React usage for components, they must follow the "Pattern"
function Home() {
    return <h1>Se você gosta de VP Pizza, dá uma risadinha!</h1>
}

// exporting the function so Next knows what to look for and render it properly
export default Home;